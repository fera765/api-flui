// Database configuration placeholder
// In-memory storage for now - ready for future database integration

export interface DatabaseConfig {
  type: 'memory';
}

export const databaseConfig: DatabaseConfig = {
  type: 'memory',
};
